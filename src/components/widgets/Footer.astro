---
import { Icon } from 'astro-icon/components';

// --- DATOS A PERSONALIZAR ---
// Ahora solo se muestran Instagram y TikTok
const socialLinks = [
  { ariaLabel: 'Instagram', icon: 'tabler:brand-instagram', href: 'https://www.instagram.com/modeldesktop.cl/' },
  { ariaLabel: 'TikTok', icon: 'tabler:brand-tiktok', href: 'https://www.tiktok.com/@model_desktop' },
];

const year = new Date().getFullYear();
---

<footer class="border-t border-primary/40">
  <div class="max-w-7xl mx-auto px-4 sm:px-6">
    <div class="grid grid-cols-1 md:grid-cols-12 gap-8 py-8 md:py-12">
      <div class="md:col-span-8 lg:col-span-7">
        <div class="mb-4">
          <a class="inline-block font-bold text-xl text-primary" href="/">ModelDesktop</a>
        </div>
        <div class="font-bold text-primary text-lg">¡Eleva tu OnlyFans!</div>
        <p class="text-sm text-default mt-2 max-w-sm">
          ¿Tienes perfil o quieres empezar en OnlyFans? Te ayudamos a crecer y destacar desde cero →
        </p>
      </div>

      <div class="md:col-span-4 lg:col-span-5">
        <div class="font-bold text-primary">Contacto</div>
        <div class="text-sm text-default mt-2">
          <p>
            <a href="tel:+56982625437" class="hover:underline">+569 8262 5437</a>
          </p>
          <p>
            <a href="mailto:contacto@modeldesktop.cl" class="hover:underline">contacto@modeldesktop.cl</a>
          </p>
        </div>
      </div>
    </div>

    <div class="md:flex md:items-center md:justify-between py-6 md:py-8">
      <div class="text-sm text-default">
        © {year} ModelDesktop, Todos los derechos reservados.
      </div>

      <ul class="flex mt-4 md:mt-0">
        {
          socialLinks.map(({ ariaLabel, href, icon }) => (
            <li class="ml-2">
              <a
                class="text-primary hover:opacity-80 transition duration-150 ease-in-out p-1"
                href={href}
                aria-label={ariaLabel}
                target="_blank"
                rel="noopener noreferrer"
              >
                <Icon name={icon} class="w-6 h-6" />
              </a>
            </li>
          ))
        }
      </ul>
    </div>
  </div>
</footer>
